#['ENUMERATE()']
from collections import Counter
class Solution:
    def firstUniqChar(self, s: str) -> int:
        count=Counter(s)
        for i,ch in enumerate(s):
            if count[ch]==1:
                return i 
        return -1 


#['WITHOUT ENUMARETE()].USING [KEY,VALUE].
from collections import Counter
class Solution:
    def firstUniqChar(self, s: str) -> int:
        count=Counter(s)
        for i in range(len(s)):
             if count[nums[i]]==1: #(count[key]==value)
                  return i
      return -1
